package studio.organon.server.review.dto;

/** Sin anotaciones de validacion: el servicio la valida y responde con un mensaje pensado para el lector. */
public record AnswerRequest(String answer) {
}

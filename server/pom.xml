<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>studio.organon</groupId>
	<artifactId>organon-server</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>organon-server</name>
	<description>Organon Studio - IDRE backend</description>
	<properties>
		<java.version>25</java.version>

		<!--
			Las pruebas etiquetadas como "integracion" exigen un PostgreSQL vivo,
			asi que quedan fuera de la ejecucion por defecto. Para incluirlas:
			mvnw test -Dexcluded.test.groups=
		-->
		<excluded.test.groups>integracion</excluded.test.groups>

		<!--
			Spring AI 2.0.1 declara spring-boot 4.1.1 en sus starters, la misma
			version que usa este proyecto, asi que no hay conflicto de BOM.
		-->
		<spring-ai.version>2.0.1</spring-ai.version>

		<!--
			Directorio donde el JDK crea el socket AF_UNIX de la tuberia de
			despertar del Selector de NIO.

			Por defecto el JDK lo pone en java.io.tmpdir. En maquinas Windows
			con antivirus que vigila %LOCALAPPDATA% el fichero de socket queda
			inaccesible nada mas crearse: el bind funciona, el connect falla con
			EINVAL y Tomcat no arranca ("Unable to establish loopback
			connection"). Apuntarlo fuera de esa carpeta lo evita.

			Es seguro en cualquier plataforma: si el directorio no existe, el
			bind AF_UNIX falla y PipeImpl cae solo al loopback TCP, que es el
			comportamiento historico.
		-->
		<organon.uds.tmpdir>${user.home}/.organon-uds</organon.uds.tmpdir>
		<organon.jvm.args>-Djdk.net.unixdomain.tmpdir=${organon.uds.tmpdir}</organon.jvm.args>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-ollama</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<jvmArguments>${organon.jvm.args}</jvmArguments>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>${organon.jvm.args}</argLine>
					<excludedGroups>${excluded.test.groups}</excludedGroups>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
